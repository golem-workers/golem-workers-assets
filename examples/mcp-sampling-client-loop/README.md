# MCP sampling client loop fixture

Dependency-free policy fixture for a client-controlled MCP sampling loop. It demonstrates capability checks, request budgets, tool allowlists, balanced tool results, recursion limits, and a final no-tools turn.

```bash
npm test
npm run demo
```

This is a policy harness, not a transport or an official MCP SDK implementation.
